# Starcatcher — Forge 1.20.1 → Fabric 1.20.1 Backport Plan

Status: **P0-P7 done** (P7 except the selling-bin subsystem, tracked under P8/§7bis.3) — see §13. In progress on branch `fabric-port-1.20.1`. Next: P8 (Companion-mod resolution) or P9 (QA).
Target: Minecraft **1.20.1**, loader **Fabric**
Source branch: `v2.3-1.20.1` (Forge, MinecraftForge 47.4.0, LegacyForge ModDev)
Scope: 335 Java files, 719 resource files, 9 lang files, 13 datagen providers, 9 network payloads, 9 screen/menu files.

---

## 0. The central problem: this is not a "plain" Forge mod

Although it runs on Forge 1.20.1, the code is written almost entirely against a **NeoForge-1.21-style API** that is emulated on Forge by the **NeoBackports** library (`net.nikdo53.neobackports.*`, 66+ import sites). Fabric has **no NeoBackports**. So the port is really two migrations at once:

1. Forge loader/runtime → Fabric loader/runtime (metadata, entrypoints, events, registries).
2. NeoBackports' emulated modern APIs (DeferredHolder, StreamCodec/ByteBufCodecs, **DataComponents**, CustomPacketPayload/IPayloadContext, **DataAttachment/AttachmentType**, data maps, custom registries) → Fabric-native or Fabric-shimmed equivalents.

**Strategic decision (recommended):** build a thin **Fabric compatibility shim** package (`com.wdiscute.starcatcher.fabricshim`, or reuse existing package names) that reproduces the *subset* of the NeoBackports/Forge API the mod actually calls, backed by Fabric APIs + a handful of libraries. This keeps most of the 335 call sites unchanged and concentrates the risk. A minority of subsystems (custom registries, data maps, capabilities, loot modifiers, GLM, config, curios) must be genuinely rewritten because no drop-in Fabric analogue exists.

Alternative (not recommended): rewrite every call site to native Fabric APIs. Far more churn across 335 files.

---

## 1. Pre-flight decisions (resolve BEFORE coding)

These gate the whole effort. Several are external-dependency availability questions.

| # | Decision | Options | Notes / recommendation |
|---|----------|---------|------------------------|
| D1 | **Data Components** (1.21 concept, used via NeoBackports on NBT) | (a) port NeoBackports' NBT-backed component impl into a shim; (b) rewrite all component reads/writes to raw NBT | Components are pervasive (`SCDataComponents`, ItemContainerContents, tackle/skin/modifier data on stacks). Recommend (a): a small NBT-backed `DataComponentType`/`get`/`set` shim. |
| D2 | **Attachments** (capability-backed entity/player data, sync + copy-on-death) | Cardinal Components API (Fabric) | 3 attachments: FishingBob (entity+player, synced), FishingGuide (player, serialized+synced+copyOnDeath), TackleSkin (entity, serialized+synced). Cardinal Components maps cleanly. |
| D3 | **Config** (`ForgeConfigSpec`, client + server specs) | **Forge Config API Port (Fabric)** by Fuzs | Provides `ForgeConfigSpec` verbatim on Fabric 1.20.1 → `SCConfig.java` needs almost no change. Recommended for Starcatcher to avoid rewriting `SCConfig`. Note: wd's own Fabric Selling-Bin instead uses **Cloth Config** (§7bis.3) — a valid alternative, but it would require rewriting `SCConfig` from the `ForgeConfigSpec` builder. |
| D4 | **Networking** (CustomPacketPayload/IPayloadContext/PayloadRegistrar) | Fabric Networking API v1 shim | 1.20.1 Fabric has channel+PacketByteBuf networking, not 1.20.5 payloads. Build a payload shim over `Server/ClientPlayNetworking`. 9 payloads. |
| D5 | **Curios compat** | **Trinkets** (or Accessories) for Fabric | Curios has no 1.20.1 Fabric build. `compat/curios/*` (4 files: hat item, renderer, compat, events) must be rewritten against Trinkets API, or gated off. |
| D6 | **Companion mods bundled/depended** — Fabric 1.20.1 status **resolved**, see §7bis | libtooltips, Ember's Text API, wds-selling-bin, tiny-multiblock-lib | tiny-multiblock-lib ✅ Fabric drop-in; Ember's Text API ✅ Fabric 1.20.1 → reimplement libtooltips facade on it; **wds-selling-bin → DECIDED: downport wd's own `1.21.1-fabric` branch to `1.20.1-fabric`** (native rewrite, same package, also a template). |
| D7 | **tiny-multiblock-lib** (aquarium + stand + telescope + trophy multiblocks) | **Use official Fabric build** — RESOLVED | Fabric 1.20.1 build exists by **Nikdo53** (same author, same `net.nikdo53.tinymultiblocklib` package) → expected **drop-in**. Just swap the Modrinth coordinate to the Fabric artifact; verify API parity of `AbstractMultiBlock`/`IMultiBlock`/`IPreviewableMultiblock`/`AbstractMultiBlockEntity`/`IBlockPosOffsetEnum`. |
| D8 | **Mappings** | Mojmap + Parchment via Fabric Loom | Code uses Mojang names already → use `loom` with official mappings layered with Parchment `2023.09.03`. Avoids Yarn renames. |
| D9 | **Selling bin / seasons / JEI / EMI / FTB / Iris** compat | swap to Fabric artifacts, gate by `FabricLoader.isModLoaded` | JEI & EMI have Fabric builds with near-identical APIs. Serene Seasons/Ecliptic/TFC/FTB/Iris(→Iris) all Fabric-available. |

> Action: confirm D1–D7 (especially **D6/D7** — external availability) with the user before implementation, since they can change scope from "large" to "blocked."

---

## 2. Build system & project scaffolding — ✅ DONE (2026-08-31, branch `fabric-port-1.20.1`)

Replaced the LegacyForge ModDev toolchain with **Fabric Loom**.

- [x] `settings.gradle`: added `maven.fabricmc.net` to `pluginManagement`; bumped `foojay-resolver-convention` **0.9.0 → 1.0.0** (0.9.0 references `JvmVendorSpec.IBM_SEMERU`, removed in Gradle 9.x → hard `compileJava` failure; see gotcha below).
- [x] `build.gradle`: replaced `net.neoforged.moddev.legacyforge` with **`net.fabricmc.fabric-loom-remap`** (Loom's plugin id as of 2026 — not the old `fabric-loom` id; confirmed against FabricMC's own live `fabric-example-mod` 1.20.1 branch). Removed `legacyForge {…}`, `obfuscation`/`reobfJar`/`generateModMetadata`.
- [x] Dependencies (all coordinates verified resolvable via `./gradlew dependencies --configuration modImplementation`):
  - `minecraft "com.mojang:minecraft:1.20.1"`, `mappings loom.layered { officialMojangMappings(); parchment("org.parchmentmc.data:parchment-1.20.1:2023.09.03@zip") }`.
  - `fabric-loader:0.19.5`, `fabric-api:0.92.11+1.20.1`.
  - Cardinal Components `5.2.3` (D2), Forge Config API Port `v8.0.3-1.20.1-Fabric` (D3, via `maven.modrinth:forge-config-api-port:…`), Trinkets `3.7.2` (D5), tiny-multiblock-lib `fabric-1.20.1-3.2.0` bundled via `include(...)` (§7bis.2), mixinextras-fabric `0.5.5`.
  - Compat-module libs (JEI/EMI/Seasons/Curios-era/FTB/TFC/Oculus/quality-food/relics) deliberately **deferred to P7** (§8) to keep P0 minimal — repos for them already present from the Forge build.gradle.
- [x] Repositories: FabricMC, Parchment, Ladysnake (Cardinal), Modrinth maven (covers Trinkets/Forge Config API Port/tiny-multiblock-lib), TerraformersMC, BlameJared (JEI), Tyson the Ember (for the future libtooltips facade, §7bis.1), cursemaven.
- [x] `gradle.properties`: retained `mod_*` props; replaced `forge_version*`/`loader_version_range` with `loader_version`/`loom_version`/`fabric_api_version` + library version props; dropped `neobackports_version`.
- [x] Mixin: removed the manual `mixin { add … config … }` block and jar `MixinConfigs` manifest attribute (Loom auto-handles refmap). Split into `starcatcher.mixins.json` (common: `GetNameMixin`, `LecternReturnMixin`, `RemoveFishSizeAndWeightWhenStacking`) + `starcatcher.client.mixins.json` (`RenderTypeHelperMixin`), referenced from `fabric.mod.json`'s `mixins` array with `"environment": "client"` — matches current Fabric convention (no `refmap`/`minVersion` keys needed).
- [x] Kept `withSourcesJar`; dropped `withJavadocJar` (not part of the standard Fabric template, low value). Dropped `jarJar`; tiny-multiblock-lib now bundled via Loom `include(...)`.
- [x] **Gradle wrapper bumped 9.2.1 → 9.7.1** (needed for the current Loom snapshot's `plugin.api-version` requirement).

**Toolchain gotchas hit & fixed (record for next environment refresh):**
1. `org.gradle.toolchains.foojay-resolver-convention:0.9.0` → **`compileJava` fails** with `JvmVendorSpec does not have member field 'IBM_SEMERU'` on Gradle 9.x (field removed). Fix: bump to `1.0.0`+.
2. Loom's run-config DSL uses `vmArg`/`programArg`, **not** `systemProperty`/`programArgs` (those are LegacyForge ModDev names).
3. **Datamaps Refabricated is not a published dependency** — see §5.6, must be vendored from source.

Deliverable **met**: `./gradlew tasks` and `./gradlew dependencies --configuration modImplementation` both succeed (all P0 dependency coordinates resolve). `./gradlew compileJava` fails with exactly the expected ~100+ "package net.minecraftforge.* / net.nikdo53.neobackports.* does not exist" errors from the untouched 335 Forge-API source files (P1+ work) — the two new entrypoint stub files compile cleanly with zero errors, confirming the Loom scaffolding itself is sound.

---

## 3. Mod metadata & manifest — ✅ DONE (2026-08-31)

- [x] Deleted `src/main/templates/META-INF/mods.toml` (and the empty `templates/` dir) and the `generateModMetadata` task; created `src/main/resources/fabric.mod.json`:
  - `id`, `version` (`${version}` via `processResources` `filesMatching("fabric.mod.json") { expand(...) }`), `name`, `description` (full credits ported verbatim from `mods.toml`), `authors`, `license`, `icon`.
  - `environment: "*"`.
  - `entrypoints`: `main` → `com.wdiscute.starcatcher.StarcatcherFabric`, `client` → `…StarcatcherFabricClient` (both new empty stubs, §4). `fabric-datagen`/`emi`/`jei` entrypoints deferred to P6/P7 when those subsystems are actually wired in.
  - `mixins`: `starcatcher.mixins.json` + `{ "config": "starcatcher.client.mixins.json", "environment": "client" }`.
  - `accessWidener`: `starcatcher.accesswidener`.
  - `depends`: kept to only what's certain right now — `fabricloader >=0.19.5`, `fabric-api`, `minecraft ~1.20.1`, `java >=17`. **TODO (P1+):** add exact mod-id entries for `cardinal-components-base`/`trinkets`/`forgeconfigapiport` once those libs are actually wired into code and their real declared ids are confirmed at runtime (didn't want to guess wrong ids and hard-fail loading before they're even used).
- [x] **Access Transformer → Access Widener** (`src/main/resources/starcatcher.accesswidener`, `accessWidener v2 named`). Translated all 6 AT entries using the Mojmap names already recorded as trailing comments in the original `accesstransformer.cfg` (`Gui.overlayMessageTime`, `ShapedRecipe.result`, `ShapelessRecipe.result`, `RegistriesDatapackGenerator.dumpValue`, `RegistryOps.<init>`, `RegistryOps.lookupProvider`) — field/method descriptors written out explicitly. Original `accesstransformer.cfg` left in place (unreferenced by the build) as a historical cross-reference. Datagen-only entries can be pruned later if datagen is rewritten (§9).

---

## 4. Core loader glue (entrypoints & registry bootstrap)

- [x] **Stub only (P0 scope)**: `StarcatcherFabric implements ModInitializer` and `StarcatcherFabricClient implements ClientModInitializer` created as empty entrypoints (logger init only) so the mod jar/manifest is structurally valid. Both compile cleanly against pure Fabric API (no dependency on the still-Forge `Starcatcher.java`).
- [ ] **P1+**: Wire the real subsystem `register()` calls into `StarcatcherFabric.onInitialize()` (Items, Blocks, BlockEntities, DataComponents, Sounds, Entities, Particles, Recipes, MenuTypes, DataAttachments, custom registries, criterion triggers, processors, loot modifiers, data maps, payloads, creative tabs) — replaces the `@Mod`-annotated constructor. On Fabric there is no `IEventBus` — the shim's `DeferredRegister.register(bus)` becomes `DeferredRegister.registerAll()` that flushes into vanilla registries directly.
- [x] **P5**: ✅ done (2026-09-01). `SCClientEvents`/`SCClientForgeEvents`/`TooltipEvents` (renderers, layers, screens, particles, keymaps, item properties, tooltip components, item tooltips) converted to plain methods called from `StarcatcherFabricClient.onInitializeClient()` — see §6 for the per-event mapping. `SCItemProperties.addCustomItemProperties()` was already pure vanilla `ItemProperties.register`, just needed calling. `SCRenderTypes`' `RegisterShadersEvent` converted to Fabric's `CoreShaderRegistrationCallback`. The 7 real `@OnlyIn(Dist.CLIENT)` sites (`FishingBobEntity` had only a dead unused import; `DisplayBlock`, `TelescopeBlock`, `FishingGuideItem`, `SecretNote`, `LetterItem`, `FishingStartedPayload`) flattened to `@Environment(EnvType.CLIENT)`. `Starcatcher.Client.init()` (tooltip-tag-processor registration) was **not** folded in — it's 100% `com.wdiscute.libtooltips`-dependent, and that companion library isn't ported yet (§7bis.1); tracked there, not here. Error count 358→274 (−84).
- [ ] **P1+**: Strip `Starcatcher.java` down to a constants/util holder (MOD_ID, `rl()`, resource keys, toast helper) — remove the Forge constructor, `@Mod`, and all `net.minecraftforge`/NeoBackports imports (currently still Forge-shaped; this is the bulk of why `compileJava` fails right now).

---

## 5. The NeoBackports/Forge API shim layer

**Decided (2026-08-31): shadow the existing package names** (`net.nikdo53.neobackports.*`, and the handful of `net.minecraftforge.*` classes actually used) rather than a new `com.wdiscute.starcatcher.fabricshim` package — at 335 files, mass-editing every import statement is far riskier than writing local classes under the same import paths. Shim source lives under `src/main/java/net/nikdo53/neobackports/…` and `src/main/java/net/minecraftforge/…`, same as any other source root. Reproduce only what is used.

### 5.1 Registries & Deferred* (66 sites) — ✅ DONE (2026-08-31)
- [x] `DeferredHolder<R, T extends R>` ([DeferredHolder.java](src/main/java/net/nikdo53/neobackports/registry/DeferredHolder.java)): implements `Supplier<T>`; `.get()`/`.value()`/`.getKey()`/`.getId()`/`.isBound()`/`.asHolder()`. Deliberately does **not** implement vanilla `Holder<R>` itself (that interface has a large abstract surface) — `.asHolder()` hands out the real vanilla `Holder<R>` (via `Registry.wrapAsHolder`) for the rare site that needs one. `bind(Registry<R>)` performs the actual `Registry.register(...)`.
  - [x] `DeferredItem<T extends Item>` / `DeferredBlock<T extends Block>` — thin subclasses fixing `R` to `Item`/`Block`.
  - [x] `DeferredRegisterTyped<T>` ([DeferredRegisterTyped.java](src/main/java/net/nikdo53/neobackports/registry/DeferredRegisterTyped.java)): `.create(ResourceKey<? extends Registry<T>>, modid)` and `.create(Registry<T>, modid)` overloads (both patterns are used in the wild — `Registries.MENU`/`ForgeRegistries.Keys...` are keys, `BuiltInRegistries.BLOCK_ENTITY_TYPE`/`.SOUND_EVENT`/`.PARTICLE_TYPE` are resolved registries). `.register(name, supplier)` only **collects** — it does not touch the real registry. `.register(IEventBus)` performs the actual flush (see below). Nested `Items`/`Blocks` subclasses (`createItems`/`createBlocks`) override `register(...)` covariantly to hand back `DeferredItem`/`DeferredBlock`.
  - [x] `net.minecraftforge.registries.DeferredRegister<T>` kept as a separate class (thin subclass of `DeferredRegisterTyped<T>`) purely so that import path still resolves — Forge 47.x's own `DeferredRegister` and NeoBackports' `DeferredRegisterTyped` have identical shape in this codebase's actual usage.
  - [x] **`IEventBus` is a deliberate no-op token** ([IEventBus.java](src/main/java/net/minecraftforge/eventbus/api/IEventBus.java)) — Fabric's `ModInitializer` has no staged lifecycle, so `DeferredRegisterTyped.register(bus)` ignores the bus and flushes **synchronously**. This means **call order in `StarcatcherFabric.onInitialize()` now encodes the dependency graph** that Forge's staged `RegisterEvent` firing order used to handle implicitly (blocks before block-items/block-entities, since their suppliers call `SCBlocks.X.get()`). Verified order: Blocks → Items → BlockEntities → Entities → Sounds → Particles → MenuTypes → CreativeModeTabs → CriterionTriggers. `IEventBus.addListener(Consumer)` is a true no-op; the one real call site (`SCCriterionTriggers`, `FMLCommonSetupEvent`) was rewritten to call its logic directly instead (see §6 — this is the only source file P1 had to actually edit, not just shim around).
- [x] **Verified against the real compiler**, not just by inspection: `./gradlew compileJava` (with `-Xmaxerrs 10000` now set in `build.gradle` for future debugging) shows **zero errors** in `SCBlocks`, `SCItems`, `SCBlockEntities`, `SCEntities`, `SCSounds`, `SCParticles`, `SCMenuTypes`, `SCCreativeModeTabs`, `SCCriterionTriggers`, or `StarcatcherFabric` — confirming `Registry.register(Registry<? super T>, ResourceLocation, T)`, `Registry<T>.wrapAsHolder(T)`, `Registry<T>.key()`, and `BuiltInRegistries.REGISTRY.get(ResourceLocation)` all resolve exactly as assumed against real Mojmap 1.20.1 + Parchment.
- [x] **Custom registries** (`FISH_RESTRICTIONS`, `MINIGAME_MODIFIERS`, `SWEET_SPOT_BEHAVIOUR`, `CATCH_MODIFIERS`, `TACKLE_SKIN`) — ✅ DONE (2026-09-01, P4). New [SCCustomRegistries.java](src/main/java/com/wdiscute/starcatcher/registry/SCCustomRegistries.java) replaces `SCModEvents.addRegistry` (`NewRegistryEvent`): `FabricRegistryBuilder.createSimple(key).buildAndRegister()` for each of the 5, assigned to `Starcatcher`'s static `_REGISTRY` fields (unavoidably touched — see below). Must run **before** `SCFishRestrictions`/`SCMinigameModifiers`/`SCSweetSpotsBehaviour`/`SCCatchModifiers`/`SCTackleSkins`' own `.register(bus)` calls, since `DeferredRegisterTyped.resolveRegistry()` looks the registry up from `BuiltInRegistries.REGISTRY` by the key's location and throws if it isn't there yet — confirmed by reading that method's actual source rather than assuming. `FabricRegistryBuilder.createSimple(...).buildAndRegister()` registers into `BuiltInRegistries.REGISTRY` early enough (mod init, long before any `RegistryAccess` snapshot at world/server bootstrap) for `level.registryAccess().registryOrThrow(key)` call sites elsewhere to pick the registry up automatically — no changes needed at any of those sites.
  - **`Starcatcher.java` turned out not to be 100% out-of-scope after all**: its 5 `IForgeRegistry<T>` static fields (`FISH_RESTRICTIONS_REGISTRY` etc.) are genuinely read by 2 in-scope files (`AbstractFishRestriction.getCodecOrThrow`, `AbstractMinigameModifier`'s dispatch codec) — real Forge type `IForgeRegistry` doesn't exist as a shim at all, so those field *declarations* had to change to vanilla `Registry<T>` (and the 2 read sites' `.getValue(loc)` → `.get(loc)`) even though the rest of that file (constructor, `@Mod` annotation, FML imports) stays untouched and broken, same as before. A useful lesson: "leave this whole file alone" calls should be re-checked once a symbol declared in that file turns out to be read from somewhere in-scope — grep the field name project-wide before assuming a file is fully inert.
- [x] `ForgeRegistries`/`NeoForgeRegistries` lookups → vanilla `BuiltInRegistries` / `Registries` keys — confirmed via grep this was **already done** (no live `ForgeRegistries`/`NeoForgeRegistries` references remain anywhere in the 335-file tree outside the untouched `Starcatcher.java`/datagen files), nothing left to do here.

### 5.2 Data Components (D1) — ✅ DONE (2026-08-31)
- [x] **Root problem discovered, not assumed**: `SCDataComponents.set/get/has/remove/getOrDefault` (and a handful of call sites elsewhere) call `stack.set(component.get(), data)` / `stack.get(...)` etc. **as instance methods directly on `ItemStack`** — real NeoForge 1.20.5+/NeoBackports patches vanilla's `ItemStack` class itself to add these. That's fundamentally impossible to reproduce in a Fabric Loom mod: Mixin can inject real bytecode into `ItemStack.class` at runtime, but **javac compiles our source against the unmodified real Mojmap `ItemStack` jar**, entirely before any Mixin transformer runs — there is no mechanism to make `stack.set(...)` compile in arbitrary call-site source files no matter what shim/duck-interface trick is used. So this had to be a **call-site rewrite of the accessor style**, not a shimmed method.
- [x] `DataComponentType<T>` ([DataComponentType.java](src/main/java/net/nikdo53/neobackports/io/components/DataComponentType.java)) — vanilla 1.20.1 predates the real Data Components system (1.20.5+) so there's no registry to back it; just pairs an id with the `Codec<T>` used for storage. `Builder<T>.persistent(Codec<T>)` matches the one builder method actually used.
- [x] `DeferredRegisterTyped.DataComponents` + `DeferredRegisterTyped.createDataComponents(modid)` ([DeferredRegisterTyped.java](src/main/java/net/nikdo53/neobackports/registry/DeferredRegisterTyped.java)) — unlike `Items`/`Blocks` there's no real registry to flush against, so `registerComponentType(name, builderOp)` constructs and binds the `DataComponentType` immediately via a new `DeferredHolder.bound(key, value)` factory (added to [DeferredHolder.java](src/main/java/net/nikdo53/neobackports/registry/DeferredHolder.java)) rather than the normal `.bind(Registry)` path; `.register(IEventBus)` is a no-op.
- [x] `ItemContainerContents` ([ItemContainerContents.java](src/main/java/net/nikdo53/neobackports/io/components/ItemContainerContents.java)) — immutable fixed-order stack list (`EMPTY`, `fromItems`, `nonEmptyItems`, `copyInto`, plus a `Codec` for storage), used only by the tackle box's own custom inventory — no vanilla interop concern, so it's fine as a wholly custom concept.
- [x] `SCDataComponents.set/get/has/remove/getOrDefault` **reimplemented over real ItemStack NBT** (`stack.getOrCreateTagElement("StarcatcherComponents")` — verified real vanilla `ItemStack` methods via `javap`: `hasTag`/`getTag`/`getOrCreateTag`/`getOrCreateTagElement`/`getTagElement`/`removeTagKey`/`setTag` all exist as expected), each component's value keyed by its `id().toString()` and (de)serialized via `type.codec().encodeStart(NbtOps.INSTANCE, data)` / `.parse(NbtOps.INSTANCE, tag)`. Added a `CONTAINER` entry (backing the tackle box's item list) alongside the existing custom components (`BUCKETED_FISH`, `SIGNED_GUIDE`, etc.) — same storage mechanism, no special-casing needed since it has no vanilla equivalent to interop with.
- [x] **`CUSTOM_NAME`/`ITEM_NAME` — not shimmed as generic components, mapped to real vanilla methods instead**, since they exist for interop (anvil renaming, tooltip rendering) that a generic NBT blob wouldn't preserve:
  - `StarcaughtBucket.getName()` used `DataComponents.CUSTOM_NAME`/`.ITEM_NAME` to manually replicate exactly the fallback chain vanilla's own `ItemStack.getHoverName()` already implements (custom name → item's own `getName()`, which itself falls back to `Component.translatable(getDescriptionId())`) — collapsed the whole 3-variable block to a single `ssc.stack().getHoverName()` call, eliminating the need to shim those two constants at all.
  - `TackleBoxBlockEntity.applyImplicitComponents`/`.saveToItem` (`ItemStack` params — same non-existent-instance-method issue) → `componentInput.hasCustomHoverName() ? componentInput.getHoverName() : null` / `stack.setHoverName(this.name)` (both real vanilla `ItemStack` methods, verified via `javap`), `CONTAINER`/`TACKLE_BOX_FISHES` access rewritten to the `SCDataComponents` static-method facade. Note: this file (and `applyImplicitComponents`/`saveToItem` themselves, which aren't real vanilla `BlockEntity` overrides in 1.20.1 either — that item↔block-entity data hook is also a 1.20.5+ addition) stays blocked by unrelated Capabilities (`net.minecraftforge.common.capabilities`/`net.minecraftforge.items`) and Events, not by anything in this sub-phase; edits here are correct but won't be exercised/verified end-to-end until those land.
  - `TackleBoxBlock.appendHoverText`'s two `CONTAINER` reads → `SCDataComponents.has`/`.getOrDefault(stack, SCDataComponents.CONTAINER, ...)`.
- [x] **Unrelated but high-leverage fix bundled in**: `javax.annotation.Nullable`/`Nonnull` (JSR-305) don't resolve anywhere in the project — never an issue on Forge (pulled in transitively), but nothing on the new Fabric dependency set provides it. Added `compileOnly "com.google.code.findbugs:jsr305:3.0.2"` to `build.gradle`; this alone cleared ~64 cascading errors across unrelated files.
- [x] `RemoveFishSizeAndWeightWhenStacking` mixin — already called only through the `SCDataComponents` facade (`.remove(stack, CAUGHT_FISH_INFO)`), needed no changes; compiles clean now that the facade does.
- [x] **Verified against the real compiler**: full-project `compileJava` error count dropped **619 → 577** (component shim) **→ 555** (after the jsr305 fix); zero errors in any new/edited file in this sub-phase (`DataComponentType`, `ItemContainerContents`, `DeferredHolder`, `DeferredRegisterTyped`, `SCDataComponents`, `StarcaughtBucket`, `RemoveFishSizeAndWeightWhenStacking` all fully clean; `TackleBoxBlock`/`TackleBoxBlockEntity`'s remaining errors are 100% attributable to Capabilities/Events, confirmed by re-grepping the error log for the resolved symbol names).

### 5.3 Codecs — ✅ DONE (2026-08-31)
- [x] `StreamCodec<V>` ([StreamCodec.java](src/main/java/net/nikdo53/neobackports/io/StreamCodec.java)) — single-type-param as predicted (buffer fixed to `FriendlyByteBuf`), `.apply(CodecOperation)`, `.map(to,from)`, `composite(...)` for arity 1–6. Confirmed this shape is exactly right by reading the mod's own pre-existing `com.wdiscute.starcatcher.io.ExtraComposites` (arities 6–13, for records with more fields than `StreamCodec.composite` supports) — it already assumed precisely this interface, independent confirmation the design was correct before it was even compiled.
- [x] `ByteBufCodecs` ([ByteBufCodecs.java](src/main/java/net/nikdo53/neobackports/io/utils/ByteBufCodecs.java)) — all surveyed members: `BOOL`/`INT`/`VAR_INT`/`VAR_LONG`/`FLOAT`/`DOUBLE`/`STRING`/`STRING_UTF8`/`UUID`/`RESOURCE_LOCATION`/`ITEM_STACK`/`INGREDIENT`/`BLOCK_POS` (all thin wrappers over real `FriendlyByteBuf` methods — `writeItem`/`readItem`, `Ingredient.toNetwork`/`fromNetwork`, `writeUUID`/`readUUID` etc., all verified to exist via `javap` against the actual mapped Minecraft jar, not assumed from memory), `list()` (a `CodecOperation`), `map(factory, keyCodec, valueCodec)`, `fromCodec(Codec<V>)` (round-trips through NBT, wrapped in a single-entry CompoundTag since the encoded `Tag` isn't always itself a CompoundTag and `FriendlyByteBuf` only exposes a CompoundTag-typed read/write), `holderRegistry(ResourceKey<? extends Registry<T>>)` (encodes a `Holder<T>` by numeric registry id via `IdMap.getId`/`byId`).
- [x] `NeoForgeStreamCodecs.enumCodec(Class<E>)` ([NeoForgeStreamCodecs.java](src/main/java/net/nikdo53/neobackports/io/utils/NeoForgeStreamCodecs.java)) — ordinal-as-varint.
- [x] `BackportCodecs` ([BackportCodecs.java](src/main/java/net/nikdo53/neobackports/io/utils/BackportCodecs.java)) — `IngredientCodecs.CODEC` (vanilla 1.20.1 `Ingredient` has no native `Codec`, that's 1.20.5+; round-trips through `Ingredient.fromJson`/`.toJson()` via `Codec.PASSTHROUGH`), `ITEM_STACK_RECIPE` (small `{item, count}` RecordCodecBuilder — vanilla has no equivalent named field), `strictUnboundedMap` (delegates to DFU's real `Codec.unboundedMap`, accepting the minor loss of duplicate-key strictness).
- [x] **Two small direct call-site edits**, not shimmed — real Mojmap 1.20.1 (verified via `javap` against the mapped jar, not guessed) already has `ItemStack.CODEC` and `Registry.holderByNameCodec()` natively, just under slightly different names/shapes than NeoBackports' NeoForge-flavored aliases:
  - `FishProperties.java`: `.holderByNameCodecNeo()` → vanilla `.holderByNameCodec()` (4 sites).
  - `FishProperties.java` + `BottledLetterRecipe.java`: `ItemStack.STREAM_CODEC`/`ItemStack.OPTIONAL_STREAM_CODEC` (don't exist on vanilla `ItemStack`, unlike `ItemStack.CODEC` which does) → `ByteBufCodecs.ITEM_STACK` (3 sites).
- [x] **Verified against the real compiler**: full-project `compileJava` error count dropped **862 → 682** (−180) after this shim landed; zero errors in any of the 4 new shim files; 6 more files went fully clean (`ExtraComposites`, `FishingGuideAttachment`, `BaitRestriction`, `Tournament`, `TournamentPlayerScore`, `TournamentSettings`) — the rest of the previously-codec-blocked files (`FishProperties`, `BottledLetterRecipe`, payload classes, ...) now fail only on **other, later-phase** shims (events, recipe-serializer shim, networking context types, attachments) — confirming the codec layer itself is no longer their blocker.

### 5.4 Networking (D4, 9 payloads) — ✅ DONE (2026-09-01)
- [x] Shimmed `CustomPacketPayload`/`CustomPacketPayload.Type`/`IPayloadContext`/`PayloadRegistrar`/`PacketDistributorNeo` in a new `net.nikdo53.neobackports.io.networking` package, over Fabric Networking API v1's **channel-based** `ServerPlayNetworking`/`ClientPlayNetworking` (this Minecraft version predates the 1.20.5+ payload-type-registry rework — confirmed the exact method/handler signatures via `javap` against the actual remapped `fabric-networking-api-v1-903fca64` module jar under `.gradle/loom-cache/remapped_mods`, not guessed):
  - [PayloadRegistrar.java](src/main/java/net/nikdo53/neobackports/io/networking/PayloadRegistrar.java) — `playToServer` always calls `ServerPlayNetworking.registerGlobalReceiver`; `playToClient` calls `ClientPlayNetworking.registerGlobalReceiver` **only when `FabricLoader.getInstance().getEnvironmentType() == EnvType.CLIENT`** (the standard Fabric guard so a dedicated server never touches client-only classes in the same method body — safe because JVM constant-pool resolution for a method's bytecode is lazy per invoked instruction, not eager at class-load time). Both register/deregister the payload's `StreamCodec` into a package-private static `Map<ResourceLocation, StreamCodec<?>>` that `PacketDistributorNeo` reads back from.
  - [PacketDistributorNeo.java](src/main/java/net/nikdo53/neobackports/io/networking/PacketDistributorNeo.java) — only `sendToPlayer(ServerPlayer, payload)`/`sendToServer(payload)` exist (the only two shapes any call site uses, vs. upstream NeoForge's much larger distributor surface); both look up the payload's codec via `payload.type().id()` from that same map, encode into a `PacketByteBufs.create()` buffer, and call `ServerPlayNetworking.send`/`ClientPlayNetworking.send`.
  - [IPayloadContext](src/main/java/net/nikdo53/neobackports/io/networking/IPayloadContext.java)/[PayloadContextImpl](src/main/java/net/nikdo53/neobackports/io/networking/PayloadContextImpl.java) — only `enqueueWork`/`player()` are used by any `handle()` method in the codebase (grepped every call site first); `enqueueWork` delegates to an `Executor` (`Minecraft`/`MinecraftServer` both `implements Executor` via `BlockableEventLoop`, confirmed via `javap`, so `client::execute`/`server::execute` work directly as the constructor arg).
  - [CustomPacketPayload.Type](src/main/java/net/nikdo53/neobackports/io/networking/CustomPacketPayload.java) is a 2-arg `(ResourceLocation id, Class<T> payloadClass)` (unlike upstream's 1-arg) — needed so `PacketDistributorNeo` can recover the codec from a bare payload instance without a registrar handle; every payload's existing `new Type<>(rl, X.class)` call site already passed both args, so this needed zero payload-file changes.
- [x] Moved `SCModEvents.registerPayloads` (a `@SubscribeEvent` method on the still-unshimmed `RegisterPayloadHandlersEvent`) out into [SCNetworking.java](src/main/java/com/wdiscute/starcatcher/registry/SCNetworking.java), a plain `register(IEventBus bus)` method matching the sibling `SCXxx.register(bus)` pattern, body unchanged (all 9 `playToClient`/`playToServer` calls). `SCModEvents.java` itself still mixes in several other unshimmed Forge event types (data maps, datapack registry, entity attributes, pack finders) — left broken, that's P4/§5.6/§5.7 scope, not touched.
- [x] The 9 payload records (`io/network/*`, `io/network/tournament/*`) needed **zero changes** — they already matched the shim shapes exactly (record + `STREAM_CODEC` + `Type<>(rl, Class)` + `handle(IPayloadContext)`).
- [x] Found and removed a dead `import net.minecraftforge.network.PacketDistributor;` (the real, unshimmed Forge class, never actually referenced — `PacketDistributorNeo` was already used everywhere) from 8 call-site files that would otherwise still fail to compile on that import alone: `FishingBobEntity`, `FishingGuideScreen`, `FishCaughtCounter`, `FishingMinigameScreen`, `SCCommands`, `MessageWriteScreen`, `StandScreen`, `TournamentHandler`. Also removed one unrelated stray dead import (`org.antlr.v4.codegen.model.Sync`, never referenced) from `FishingBobEntity.java` found while cleaning up the same import block.
- [x] Wired `SCNetworking.register(bus)` into `StarcatcherFabric.onInitialize()`.

### 5.5 Attachments (D2, Cardinal Components) — ✅ DONE (2026-09-01)
- [x] **No standalone `AttachmentType`/`DataAttachment`/`DataAttachmentRegistry`/`AdvancedCapabilityType` shim** — grepped the whole 335-file tree and confirmed none of those types are referenced anywhere outside `SCDataAttachments.java` itself, so (same "flatten, don't shim the whole abstraction" call as §5.2/§5.8) the builder-chain API was dropped entirely and `SCDataAttachments.java` was rewritten directly against Cardinal Components:
  - [SCDataAttachments.java](src/main/java/com/wdiscute/starcatcher/io/SCDataAttachments.java) — `FISHING_BOB`/`FISHING_GUIDE`/`TACKLE_SKIN` are now `ComponentKey<AttachmentComponent<T>>` (via `ComponentRegistry.getOrCreate(id, (Class) AttachmentComponent.class)`, the documented CCA raw-cast pattern for generic component types). `get/set/remove(Entity, key)` facade methods preserved verbatim at every call site (3 stray `.get()` calls left over from the old `Supplier<AttachmentType<T>>` field type were cleaned up in `StarcatcherFishingRodItem.java` (×2) and `FishProperties.java` (×1) — everywhere else already called the facade without `.get()`).
  - [AttachmentComponent.java](src/main/java/net/nikdo53/neobackports/io/attachment/AttachmentComponent.java) — one generic `Component`/`AutoSyncedComponent`/`CopyableComponent<AttachmentComponent<T>>` implementation shared by all 3 attachments, holding an initializer `Supplier<T>`, a nullable persistence `Codec<T>`, a nullable sync `StreamCodec<T>`, and the current value. `readFromNbt`/`writeToNbt` round-trip through `NbtOps.INSTANCE` (same pattern as `SCDataComponents`); `writeSyncPacket`/`applySyncPacket` round-trip through the existing `StreamCodec`; `reset()` re-invokes the initializer (backs `SCDataAttachments.remove`, matching the original "removeData resets to default" semantics, not literal removal).
  - `.sync(streamCodec)` → `SCDataAttachments.set`/`.remove` call `key.sync(holder)` after mutating (CCA's `ComponentKey.sync(Object)`, confirmed via `javap`) — this is push-on-every-write rather than upstream NeoForge's declarative "always auto-sync" flag, but is behaviorally equivalent for every actual call site (none relied on sync-without-a-write).
  - `.copyOnDeath()` → passed as an explicit `RespawnCopyStrategy` argument at **registration** time (CCA models it as a registration-time strategy, not a per-component flag) — `ALWAYS_COPY` for `FISHING_GUIDE` (had `.copyOnDeath()`), `NEVER_COPY` for `FISHING_BOB` (didn't).
  - `.canAttachTo(...)` → not reproduced as a general predicate; grepped every real attach site instead. `FISHING_BOB`/`FISHING_GUIDE` are only ever attached to `Player`s (`registerForPlayers`); `TACKLE_SKIN` is only ever attached to `FishingBobEntity` (`registerFor(FishingBobEntity.class, ...)`) — narrower and more precise than the original's declared-but-partially-unused `NON_LIVING_ENTITY`/`PLAYER` combinations.
  - [SCEntityComponents.java](src/main/java/com/wdiscute/starcatcher/io/attachments/SCEntityComponents.java) — new `EntityComponentInitializer`, registered via the `cardinal-components-entity` entrypoint in `fabric.mod.json` (confirmed the exact entrypoint key string, `"cardinal-components-entity"`, from CCA's own decompiled `StaticEntityComponentPlugin` source) — CCA registration doesn't go through the mod's `IEventBus` token at all, so `SCDataAttachments` has no `register(IEventBus)` method (the original only existed for `DeferredRegister`, and was called only from the untouched, already out-of-scope Forge `Starcatcher.java`).
  - `FishingBobAttachment.setUuid` took a real Forge `CapabilityProvider` and called `.setData(...)` on it directly — same javac-vs-Mixin-timing impossibility as §5.2's `ItemStack` components (Forge's real `Entity extends CapabilityProvider` patch doesn't exist on Fabric). Flattened the parameter to plain `Entity` and routed the body through `SCDataAttachments.set(...)`. `FishingGuideAttachment`'s `player.setData/getData/syncData(...)` calls were flattened the same way.
- [x] Cardinal Components was already a `build.gradle` dependency from a prior session (`cardinal-components-base`/`cardinal-components-entity` 5.2.3) — no build-system changes needed this phase. (Fabric API's own native `fabric-data-attachment-api-v1` module was investigated and rejected: it exists in this MC version but has no sync support at all, and confirmed via `javap` that `Entity` does not implement its `AttachmentTarget` interface at this project's compile-time — Loom's compile-time interface-injection isn't active here — so it would've needed the same kind of manual sync machinery CCA already provides for free.)
- [x] Added the `cardinal-components-entity` entrypoint to `fabric.mod.json`.

### 5.6 Data Maps (`SCDataMaps`, `RegisterDataMapTypesEvent`) — ✅ DONE (2026-09-01, P4, except `ItemAttributeModifierEvent`)
- [x] **Decided against vendoring Datamaps Refabricated, reimplemented from scratch instead.** Re-checked the library before committing to the clone-and-build plan: it's a full, unfamiliar Fabric Loom mod project with **0 stars, 8 commits, no README, no releases, no Maven coordinate** — cloning and building an entire unreleased Loom project from source just to get one jar is a large, uncertain time cost. More decisively: it almost certainly patches vanilla `Holder<T>` to add `Holder.getData(DataMapType)` the same way real NeoForge does — and per §5.2/§5.5's now-repeated lesson, an instance method injected onto a vanilla class via Mixin **cannot** be made to compile against at arbitrary call sites in this project regardless of whose library provides the Mixin (javac compiles against the unmodified real jar, entirely before any Mixin transformer runs). So even a successful vendor would still have needed every `holder.getData(...)` call site flattened to a static facade — the exact same amount of call-site work either way. Given the library brings no net compile-time benefit here, it wasn't worth the build risk.
- [x] Reimplemented as a small self-contained `net.nikdo53.neobackports.datamaps` package:
  - [DataMapType.java](src/main/java/net/nikdo53/neobackports/datamaps/DataMapType.java) — id + `ResourceKey<? extends Registry<R>>` + `Codec<T>` + an internal `Map<ResourceLocation, T>` of resolved values; `Builder.synced(...)` kept only for source compatibility with the original NeoForge-style chain (**no-op** — see the sync gap noted below) so `SCDataMaps`'s existing builder-chain declarations needed **zero changes**. `.build()` self-registers the type into `DataMapRegistry`.
  - [DataMapRegistry.java](src/main/java/net/nikdo53/neobackports/datamaps/DataMapRegistry.java) — one `SimplePreparableReloadListener` per distinct registry key (`minecraft:item`, `starcatcher:fish` in this mod), lazily registered via `ResourceManagerHelper.get(PackType.SERVER_DATA).registerReloadListener(...)` (needed `IdentifiableResourceReloadListener`, not just `PreparableReloadListener` — confirmed via `javap` on the real `fabric-resource-loader-v0` module) the first time a `DataMapType` targeting that registry is built. For each registered type, resolves its **exact** expected file path — `data/<id-namespace>/data_maps/<registry-folder>/<id-path>.json`, the same convention real NeoForge data maps use (confirmed against this project's own already-generated `src/generated/resources/data/starcatcher/data_maps/...` files, e.g. `item/tackle_skin.json` for the `minecraft:item` registry and `starcatcher/fish/treasures.json` for the custom `starcatcher:fish` registry — namespace segment dropped only for `minecraft`) — and calls `ResourceManager.getResourceStack(...)` on it directly, which already returns **every loaded pack's contribution to that exact path** in priority order. This means a third-party datapack can still extend this mod's data maps by writing into the same path (the actual point of data maps) with **no directory-scanning code needed at all** — `getResourceStack` does the cross-pack part for free. Parses each layer's `"values"`/`"remove"` (supporting both a bare value and a `{"value": ..., "replace": ...}` wrapper per entry, matching real NeoForge JSON shape) via the type's own `Codec<T>` and `JsonOps.INSTANCE`.
  - `SCDataMaps.getOrDefault(ItemStack, DataMapType<Item,T>, T)` — flattened from `stack.getItemHolder().getData(dataMap)` to `dataMap.get(BuiltInRegistries.ITEM.getKey(stack.getItem()))`; all 17 existing `ItemStack`-based call sites across the tree needed **zero changes** (same signature).
  - New `SCDataMaps.getOrDefault(Holder<R>, DataMapType<R,T>, T)` overload (via `holder.unwrapKey().map(key -> dataMap.get(key.location()))`) covers the 3 `holder.getData(SCDataMaps.TREASURE)` call sites (`FishProperties.loadTreasure`, `StarcatcherEmiFPRecipe`, `StarcatcherJeiFPRecipe`) — each already constructed a `Holder<FishProperties>` via `registry.wrapAsHolder(fp)` first, so only the final `.getData(X)` → `SCDataMaps.getOrDefault(holder, X, null)` needed changing.
  - `SCDataMaps.register()` (called from `StarcatcherFabric.onInitialize()`, replacing `SCModEvents.registerAttributed(RegisterDataMapTypesEvent)`) has an empty body — merely *calling* a static method triggers the class's `<clinit>`, which is enough to run all 5 `DataMapType` builders (and therefore register their reload listeners) with zero extra ceremony.
- [x] **Known, intentional gap — no client sync of resolved values.** The original NeoForge `.synced(codec, true)` pushed resolved data-map values to *remote* multiplayer clients over the network; this reimplementation only loads from `PackType.SERVER_DATA`, which integrated-server (singleplayer) clients share automatically with their local "server" but a real dedicated-server remote client would not see correctly without an explicit sync packet — not implemented. Fine for singleplayer/local testing (the common case for this kind of gameplay data), a real gap for dedicated multiplayer; flagged here rather than silently glossed over, matching how Capabilities/Curios gaps were handled in P2/P3.
- [x] **`ItemAttributeModifierEvent`/`SCEvents.modifyItemAttribute` — still deliberately not converted.** Its original purpose (copy a data-map value onto an item's own component *once*, the first time the event fires for that stack) doesn't map onto any single Fabric hook, and the plan's own suggested alternatives (move to stack-build time, a tick, an `ItemStack` mixin, or resolve data-map values on demand inside `SCDataComponents.get`) are a real design decision, not a mechanical port — left for a future increment rather than folded into this one.
- [x] **Verified against the real compiler**: full-project `compileJava` error count dropped **368 → 358** (−10); zero errors in `DataMapType.java`, `DataMapRegistry.java`, `SCDataMaps.java`, or any of the 20 call sites touched/left alone across the tree (`AquariumBlock`, `SCCatchModifiers`, `SCMinigameModifiers`, `SCTackleSkins`, 3 recipe files, `FishProperties`, and the 2 already-broken-for-unrelated-reasons EMI/JEI files) — grepped the full error log for `SCDataMaps`/`DataMapType`/`DataMapRegistry`/`getOrDefault` and the only remaining hits are in `DGSCDataMapsProvider`/`DataGenerators` (datagen, P6, blocked by the missing `sellingbin` companion mod and unshimmed Forge datagen framework — unrelated to this sub-phase).

### 5.7 Datapack registry (`FISH_REGISTRY_KEY`, FishProperties codec) — ✅ DONE (2026-09-01, P4)
- [x] Replaced `DataPackRegistryEvent.NewRegistry`/`SCModEvents.addDatapackRegistry` with new [SCDynamicRegistries.java](src/main/java/com/wdiscute/starcatcher/registry/SCDynamicRegistries.java): `DynamicRegistries.registerSynced(Starcatcher.FISH_REGISTRY_KEY, FishProperties.CODEC)` (Fabric's single-codec overload — the original Forge call used the same `FishProperties.CODEC` for both the data and network codec slots, so this covers both). Every real call site already reaches `FISH_REGISTRY_KEY` through vanilla `RegistryAccess.registryOrThrow(...)`, so nothing downstream needed to change — confirmed via `javap` on the actual `fabric-registry-sync-v0` module jar (`net.fabricmc.fabric.api.event.registry.DynamicRegistries`), not guessed.

### 5.8 Misc NeoBackports utils — ✅ DONE (2026-08-31)
- [x] `FastColorNeo` ([FastColorNeo.java](src/main/java/net/nikdo53/neobackports/utils/FastColorNeo.java)) — delegates red/green/blue/alpha to vanilla `FastColor.ARGB32` (exists natively); adds the one NeoForge-only 2-arg `color(alpha, rgb)` overload vanilla lacks.
- [x] `CommonColorsNeo` ([CommonColorsNeo.java](src/main/java/net/nikdo53/neobackports/utils/CommonColorsNeo.java)) — only `LIGHT_GRAY` (the one constant actually referenced).
- [x] `ListReverser` ([ListReverser.java](src/main/java/net/nikdo53/neobackports/utils/ListReverser.java)) — `reverse(List<T>)` returning a new reversed copy (call sites pass immutable `.toList()` results).
- [x] **`TooltipContext` — not shimmed, flattened instead**: the 2 override sites (`FishingGuideItem`, `StarcaughtBucket`) never read anything off `context` beyond passing it to `super.appendHoverText(...)`, so the signature was changed directly to vanilla's real `Item.appendHoverText(ItemStack, Level, List<Component>, TooltipFlag)` (verified via `javap`) instead of building a NeoForge-style tooltip-context bridge.
- [x] **`ItemInteractionResult` — not shimmed, flattened instead**: `AquariumBlock`/`DisplayBlock` declared `useItemOn(ItemStack, BlockState, Level, BlockPos, Player, InteractionHand, BlockHitResult): ItemInteractionResult`, which is a 1.20.5+ Block method that **does not exist on vanilla 1.20.1 and was never actually being called by the game** — a real latent bug the port surfaced, not just a naming mismatch. Merged into vanilla's real single-method `Block.use(BlockState, Level, BlockPos, Player, InteractionHand, BlockHitResult): InteractionResult` (verified via `javap` on `BlockBehaviour`), computing the held stack via `player.getItemInHand(hand)`; `ItemInteractionResult.SUCCESS` → `InteractionResult.SUCCESS`, `.PASS_TO_DEFAULT_BLOCK_INTERACTION` → `InteractionResult.PASS`.
- [x] `IMenuTypeExtension` ([IMenuTypeExtension.java](src/main/java/net/nikdo53/neobackports/utils/IMenuTypeExtension.java)) + `net.minecraftforge.network.IContainerFactory` ([IContainerFactory.java](src/main/java/net/minecraftforge/network/IContainerFactory.java)) — Fabric API's `ExtendedScreenHandlerType`/`ExtendedFactory` (ships inside the `fabric-api` jar-in-jar bundle, confirmed via `javap` on the nested `fabric-screen-handler-api-v1` jar) has the *exact* `(windowId, Inventory, FriendlyByteBuf) -> T` factory shape Forge's `IContainerFactory` does — `IMenuTypeExtension.create(factory)` just wraps it: `new ExtendedScreenHandlerType<>(factory::create)`.
- [x] `LayeredDraw`/`LayeredDraw.Layer` ([LayeredDraw.java](src/main/java/net/nikdo53/neobackports/screen/LayeredDraw.java)) — genuinely doesn't exist in vanilla 1.20.1 (confirmed absent via `javap`/jar listing against the mapped Minecraft jar; it's a 1.20.5+ addition). Only the `Layer` functional-interface shape was reproduced so `FishRadarLayer`/`TournamentOverlay` compile; actually hooking layers into HUD rendering (replacing the Forge `RegisterGuiOverlaysEvent` call site with Fabric API's `HudRenderCallback`) is §6 (Events) work, not done here.
- [x] `BlurShaderLoader`/`BlurScreenBackports` ([BlurShaderLoader.java](src/main/java/net/nikdo53/neobackports/screen/BlurShaderLoader.java), [BlurScreenBackports.java](src/main/java/net/nikdo53/neobackports/screen/BlurScreenBackports.java)) — the real thing renders a later-version menu-background-blur effect with no 1.20.1 equivalent; `shouldCancelBackground` always returns `false` (screens fall through to normal `renderBackground`, matching stock 1.20.1), `renderBlurOrPanorama` is a no-op stub the `false` return makes unreachable in practice.
- [x] `net.minecraftforge.common.util.RecipeMatcher` ([RecipeMatcher.java](src/main/java/net/minecraftforge/common/util/RecipeMatcher.java)) — no vanilla or Fabric equivalent exists (confirmed absent from both the mapped Minecraft jar and Fabric API); reproduced as a standard Kuhn's-algorithm bipartite matcher (`findMatches(List<T>, List<? extends Predicate<T>>): int[]`) rather than reverse-engineering Forge's exact bytecode — behaviourally equivalent for the one thing the sole call site (`BottledLetterRecipe`) checks (assignment existence).
- [x] **Recipe shims (`RecipeHolder`, `*RecipeNeo`, `*RecipeInput`, `SmithingRecipeHolder`, `CraftingInput`) — not shimmed, all 4 recipe classes flattened to real vanilla 1.20.1 shapes instead** (`BottledLetterRecipe`, `NetheriteUpgradeSmithingRecipe`, `FishingRodSkinSmithingRecipe`, `TackleSkinSmithingRecipe`). This was the biggest sub-item and needed decompiled vanilla source (`ShapelessRecipe.java`, `SmithingTransformRecipe.java`, pulled from Loom's genSources jar) to get right, not just `javap` signatures:
  - `CraftingRecipeNeo`/`SmithingRecipeNeo` → vanilla `CraftingRecipe`/`SmithingRecipe` (both `extends Recipe<C>` with `C` = `CraftingContainer`/`Container`). `RecipeSerializerNeo<T>` (MapCodec+StreamCodec+`recipeHolderFactory`) → vanilla `RecipeSerializer<T>` (`fromJson(ResourceLocation, JsonObject)`/`fromNetwork(ResourceLocation, FriendlyByteBuf)`/`toNetwork`) — **this is a genuine API-generation gap, not a naming difference**: 1.20.1 has no `MapCodec`-based serializer contract, no `StreamCodec`, no separate id-carrying `RecipeHolder` wrapper (`Recipe.getId()` is still abstract on the recipe itself in 1.20.1, confirmed via `javap` — the `RecipeHolder<T>` split came later).
  - `HolderLookup.Provider` params on `assemble`/`getResultItem` → vanilla `RegistryAccess` (verified `RegistryAccess extends HolderLookup.Provider`, so the wider NeoBackports-flavored param type is not a valid override of the narrower vanilla one — had to narrow, not just rename).
  - `SmithingRecipeInput.template()/base()/addition()` → vanilla `Container.getItem(0)/getItem(1)/getItem(2)` (slot order confirmed against real vanilla `SmithingTransformRecipe` source, not assumed).
  - Existing `MapCodec`/RecordCodecBuilder field definitions were **kept and reused** (not rewritten as manual Gson parsing) — `fromJson` just does `CODEC.codec().parse(JsonOps.INSTANCE, json).getOrThrow(false, s -> {})`, since vanilla's `RecipeSerializer.fromJson` contract doesn't dictate *how* you produce a `T` from `JsonObject`, only that you do.
  - The two id-less record recipes (`NetheriteUpgradeSmithingRecipe`, `TackleSkinSmithingRecipe`) needed `ResourceLocation id` added as a record component (records can't hold extra mutable fields) — the JSON codec doesn't encode `id`, so `CODEC` was retyped to `MapCodec<Function<ResourceLocation, T>>` (a curried constructor) and `fromJson`/`fromNetwork` apply the id afterward. The one non-record recipe (`BottledLetterRecipe`) and the one plain-class smithing recipe (`FishingRodSkinSmithingRecipe`) just got a mutable `id` field instead — simpler since they're not records.
  - `IngredientExtension.CODEC_NONEMPTY`/`.CONTENTS_STREAM_CODEC` (used only in `BottledLetterRecipe`, never shimmed as its own class) → the already-verified `BackportCodecs.IngredientCodecs.CODEC`/`ByteBufCodecs.INGREDIENT` from §5.3.
  - `BottledLetterRecipe.matches` (shapeless-style, non-simple-ingredient fallback) needed real vanilla `StackedContents`/`RecipeMatcher` semantics reproduced from the decompiled `ShapelessRecipe.matches` algorithm, not guessed.
  - `SCRecipes.java` needed **zero changes** — it already declared `Supplier<RecipeSerializer<T>>` (real vanilla type) rather than the Neo variant. `SCRecipes.register(bus)` was however never wired into `StarcatcherFabric.onInitialize()` — added now, otherwise the freshly-fixed serializers would still never register.
- [x] **Verified against the real compiler**: full-project `compileJava` error count dropped **682 → 619** (−63); zero errors in any of the 9 new/edited files in this sub-phase, and grepping the full error log for every §5.8-scoped symbol name (`TooltipContext`, `ItemInteractionResult`, `RecipeSerializerNeo`, `useItemOn`, etc.) returns zero remaining hits anywhere in the 335-file tree.

---

## 6. Events: Forge bus → Fabric callbacks (7 `@SubscribeEvent` files)

Server-side/common events (`SCModEvents`, `SCEvents`) — ✅ DONE (2026-09-01, P4), converted to plain methods called from `StarcatcherFabric.onInitialize()` (`SCEntities.registerAttributes/registerSpawnPlacements`, `SCEvents.register()`) instead of a Forge event bus:

| Forge event | Fabric replacement | Status |
|---|---|---|
| `SpawnPlacementRegisterEvent` | vanilla `SpawnPlacements.register(...)` at init, widened via **access widener** (`starcatcher.accesswidener`) since it's `private` and — confirmed via full `unzip -l` scan of every fabric-api module jar — **no Fabric API equivalent exists at all** (unlike `FabricDefaultAttributeRegistry`, there's no `FabricSpawnPlacementRegistry`) | ✅ done |
| `EntityAttributeCreationEvent` | `FabricDefaultAttributeRegistry.register(FISH, FishEntity.createAttributes())` — its `AttributeSupplier.Builder` overload means the original's `.build()` call isn't even needed | ✅ done |
| `RegisterCommandsEvent` | `CommandRegistrationCallback.EVENT` | ✅ done |
| `ServerStartedEvent` / `ServerStoppingEvent` | `ServerLifecycleEvents.SERVER_STARTED` / `SERVER_STOPPING` | ✅ done |
| `TickEvent.ServerTickEvent` (END) | `ServerTickEvents.END_SERVER_TICK` — `TournamentHandler.tick(TickEvent.ServerTickEvent)` flattened to `tick(MinecraftServer)` since `event.getServer()` was the only thing it ever read off the event | ✅ done |
| `PlayerEvent.PlayerLoggedInEvent` | `ServerPlayConnectionEvents.JOIN` (`handler.player` field for the `ServerPlayer`) | ✅ done |
| `PlayerInteractEvent.RightClickBlock` (bonemeal→worms) | `UseBlockCallback.EVENT` — returns `InteractionResult.PASS` unconditionally (the original Forge handler never cancelled the event either, purely additive side effects) | ✅ done |
| `NewRegistryEvent` | §5.1 custom registries, `SCCustomRegistries` | ✅ done |
| `DataPackRegistryEvent` | §5.7 `SCDynamicRegistries` | ✅ done |
| `AddPackFindersEvent` (2 built-in datapacks) | `ResourceManagerHelper.registerBuiltinResourcePack(id, container, ...)` | ❌ **blocked, not by a missing shim**: `SCModEvents.addPackFinders` references `com.wdiscute.sellingbin.event.SBevents`, and the entire `com.wdiscute.sellingbin` package **does not exist anywhere in this Fabric port's source tree** — it's the companion Selling-Bin mod, not yet downported (§7bis.3/P8, its own separate task). Nothing to do here until that lands. |
| `ItemAttributeModifierEvent` | see §5.6 (no direct event) | ❌ deliberately not converted — needs a design decision, not a mechanical port, see §5.6 |
| `RegisterDataMapTypesEvent` | §5.6 custom loader | ✅ done (P4) — `SCDataMaps.register()` |
| `RegisterPayloadHandlersEvent` | §5.4 shim registrar at init | ✅ done (P3) |
| `ModList.get().isLoaded(id)` (16 files, not 14 as originally estimated) | `FabricLoader.getInstance().isModLoaded(id)` | ✅ done (P4) — no mod-id remapping needed anywhere (`"curios"` checks left as literal `"curios"` for now; remapping to `"trinkets"` is part of the separate §8/D5 Curios→Trinkets compat work, not this mechanical API swap) |

Client-side rendering/registration events — ✅ **done (2026-09-01, P5)**. All real Fabric API class names below confirmed via `javap` against the actual `fabric-api` module jars in `.gradle/loom-cache`, not guessed:

| Forge event | Fabric replacement | Status |
|---|---|---|
| `EntityRenderersEvent.RegisterRenderers` (block entity renderers) | `net.fabricmc.fabric.api.client.rendering.v1.BlockEntityRendererRegistry.register` | ✅ done |
| `EntityRenderersEvent.RegisterRenderers` (entity renderers) | unchanged — vanilla `net.minecraft.client.renderer.entity.EntityRenderers.register` was already what Forge's event called internally, no Fabric API needed | ✅ done |
| `RegisterMenuScreensEvent` | vanilla `net.minecraft.client.gui.screens.MenuScreens.register` directly (the NeoBackports `RegisterMenuScreensEvent` shim this used to depend on doesn't exist anywhere in this port's source tree — was itself a compile error) | ✅ done |
| `EntityRenderersEvent.RegisterLayerDefinitions` | `net.fabricmc.fabric.api.client.rendering.v1.EntityModelLayerRegistry.registerModelLayer` | ✅ done |
| Key mappings (`SCKeymappings`) | `net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper.registerKeyBinding` | ✅ done |
| `InputEvent.Key` (raw GLFW key press → `TournamentOverlay.expandedType.next()`) | no raw key-event bus on Fabric; polled instead via `KeyMapping.consumeClick()` inside `net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents.END_CLIENT_TICK` — vanilla's own input handling already tracks queued clicks once the mapping is registered | ✅ done |
| Particle providers (`SCParticles`) | `net.fabricmc.fabric.api.client.particle.v1.ParticleFactoryRegistry.getInstance().register` | ✅ done |
| Item properties (`SCItemProperties`) | unchanged — already pure vanilla `ItemProperties.register`, just needed calling from `onInitializeClient()` | ✅ done |
| `RegisterShadersEvent` (`SCRenderTypes`, 2 core shaders) | `net.fabricmc.fabric.api.client.rendering.v1.CoreShaderRegistrationCallback` — its `RegistrationContext.register(ResourceLocation, VertexFormat, Consumer<ShaderInstance>)` builds the `ShaderInstance` internally instead of handing you a `ResourceProvider`, one fewer step than Forge's version | ✅ done |
| `RegisterGuiOverlaysEvent` (fish radar, tournament overlay) | `net.fabricmc.fabric.api.client.rendering.v1.HudRenderCallback` — the overlays already implemented this port's own `LayeredDraw.Layer` shim (§5.8) whose `render(GuiGraphics, float)` shape is identical to `HudRenderCallback.onHudRender`, so it was a direct method-reference swap, `EVENT.register(instance::render)` | ✅ done |
| `RegisterClientTooltipComponentFactoriesEvent` | `net.fabricmc.fabric.api.client.rendering.v1.TooltipComponentCallback` — unlike Forge (one registration per `TooltipComponent` subtype), Fabric has a single callback returning `ClientTooltipComponent`/`null`, so both of this mod's tooltip types are dispatched via `instanceof` inside one registration | ✅ done |
| `ItemTooltipEvent` | `net.fabricmc.fabric.api.client.item.v1.ItemTooltipCallback` — note it does **not** expose the viewing entity like Forge's event did (`getTooltip(ItemStack, TooltipFlag, List<Component>)` only); call sites that used `event.getEntity()` now read `Minecraft.getInstance().player` directly instead | ✅ done |
| `DistExecutor`/`@OnlyIn`/`Dist` | `@Environment(EnvType.CLIENT)` (`net.fabricmc.api.Environment`/`EnvType`) — turned out to be **7 files**, not 12 as originally estimated (`FishingBobEntity` had only a dead unused import, already worked around via a nested `PlayerGetter` helper) | ✅ done, except `Starcatcher.java`'s `DistExecutor.safeRunWhenOn(Dist.CLIENT, Client::init)` call site — left alone since `Client.init()` itself is 100% blocked on the unported `libtooltips` (§7bis.1) and the surrounding constructor is the separate §4 "P1+: strip `Starcatcher.java`" task |
| Block/render layers, item/block color handlers (`BlockRenderLayerMap`, `ColorProviderRegistry`) | n/a — **not actually used anywhere in this mod**, confirmed by grep; the original table row guessed these were needed based on `SCRenderTypes`/`SCColors`' names alone. `SCColors` is a plain `int` constants interface with zero Forge dependency; `SCRenderTypes`' only Forge-event use was the core-shader registration above | n/a, nothing to do |

---

## 7. Creative tabs, loot modifiers, recipes, menus — ✅ DONE (2026-09-01, except the Capabilities/Curios-entangled inventory sites noted below)

- [x] **Creative tabs** (`SCCreativeModeTabs`) — ✅ turned out to need **zero changes**: it already used pure vanilla `CreativeModeTab.builder()`/`.icon()`/`.title()`/`.displayItems()`/`.build()` (all real 1.20.1 API, no NeoForge-only shape) plus the already-verified `DeferredRegisterTyped.create(Registries.CREATIVE_MODE_TAB, ...)` pattern from §5.1 — confirmed zero compiler errors in this file, not by inspection. `FabricItemGroup.builder()` (a Fabric-specific enhancement, mainly search/sync polish) was **not** needed for it to compile or function; not worth the extra dependency surface for a single tab.
- [x] **Loot modifiers** (`SCLootModifiers`, `DGSCLootModifiers`) — ✅ done (2026-09-01). Forge GLM has no Fabric equivalent, confirmed no `data/…/loot_modifiers` JSON existed in this project (it was pure-code, not datapack-driven, so nothing to delete there). Reimplemented the single existing modifier (`fishing_hat_from_shipwrecks`: 10% chance of one random hat item on the shipwreck-map table) directly as a `net.fabricmc.fabric.api.loot.v2.LootTableEvents.MODIFY` listener in `SCLootModifiers` (real signature pulled from Fabric API's own decompiled sources, not guessed) — builds a `LootPool` with one `LootItem.lootTableItem(...)` entry per hat (uniform weight) gated by `LootItemRandomChanceCondition.randomChance(0.1f)` on the pool, `tableBuilder.withPool(pool)` when `id.equals(BuiltInLootTables.SHIPWRECK_MAP) && source.isBuiltin()`. Deleted `DGSCLootModifiers`/`AddItemModifier` (the old GLM-codec + datagen-JSON pair) entirely — no longer needed since this is pure runtime code now. `SCLootModifiers.register(IEventBus)` kept as the method name/signature so the (currently-unused, legacy) call site in `Starcatcher.java` didn't need touching; wired into the real entrypoint (`StarcatcherFabric.onInitialize()`), along with `SCDataComponents.register(bus)` which had been built in §5.2 but never actually called.
- [x] **Menus** (`SCMenuTypes`) — ✅ done in §5.8: `IMenuTypeExtension.create(factory)` → Fabric API's `ExtendedScreenHandlerType` (exact factory-shape match, confirmed via `javap`). `SCMenuTypes` itself needed no changes. Screen registration (`RegisterMenuScreensEvent` → `MenuScreens.register`) is still open, tracked under §6 Events.
- [x] **Recipes** (`SCRecipes`, recipe/*) — ✅ done in §5.8: all 4 recipe classes flattened from NeoBackports' `RecipeHolder`/`*RecipeInput`/`SmithingRecipeNeo`/`CraftingRecipeNeo`/`RecipeSerializerNeo` to real vanilla 1.20.1 `Recipe<Container>`/`RecipeSerializer<T>` signatures. `SCRecipes.register(bus)` now wired into `StarcatcherFabric.onInitialize()`.
- [x] **Inventories** — ✅ done for the self-contained sites (2026-09-01): `ItemStackHandler(size)` → vanilla `SimpleContainer(size)` (anonymous-subclass `getMaxStackSize()` override replacing `ItemStackHandler`'s `getStackLimit`, since `Container`'s own default is 99 not 64), `SlotItemHandler` → vanilla `Slot` (identical `(container, index, x, y)` constructor shape, and vanilla `Slot` already has the same `mayPlace`/`getNoItemIcon`/`mayPickup` override points `SlotItemHandler` did, so the anonymous subclasses needed no changes beyond the base class name), `.getStackInSlot/.setStackInSlot` → `.getItem/.setItem`, `.getSlots()` → `.getContainerSize()`. Fixed: `StandBlockEntity.entryCost`, `FishingRodMenu.inventory` (+ its 3 `SlotItemHandler`→`Slot` slots), `StandMenu`'s slot (+ 2 `fromItemStackHandler` call sites, renamed to `SingleStackContainer.fromContainer` since the type is vanilla `Container` now).
  - **Deliberately left alone, 2 sites**: `TackleBoxBlockEntity`'s `LazyOptional<ItemStackHandler> capability` + `getCapability` override — entangled with the whole Forge Capabilities system (`Capability<T>`, `LazyOptional<T>`, `SidedInvWrapper` for hopper automation), which needs Fabric's Transfer API (`ItemStorage`, transaction-based) rather than a 1:1 type swap; this file stays blocked on that regardless of what's done here. `CuriosCompat`'s `IItemHandlerModifiable` is real Curios API (not our `ItemStackHandler`), tied to the Curios→Trinkets swap already tracked under §8/D5, not this bullet.

---

## 7bis. Companion-library resolution (investigated — findings)

Investigated the three Fabric analogs the author suggested. Verdicts:

### 7bis.1 `libtooltips` → **custom-tooltip-api does NOT fit; reimplement on Ember's Text API (Fabric)**
- Actual API the mod calls (small surface): `Tooltips.registerProcessor(name, (t,s,e)->Component)` ×9, `Tooltips.resolveTagsToComponentFromTranslationKey(key)` ×5, `Tooltips.resolveTagsToComponent(markdownString)` ×2, and `ExampleRGBEffect` (1 import in `Starcatcher.java`, appears unused → drop).
- `libtooltips` is wd's **thin wrapper around Ember's Text API** that: (a) registers named custom text-effect tag processors (`scgolden`, `sclegendary`, `scepic`, `scrare`, `scuncommon`, `sclava`, …), and (b) resolves an XML/markdown-tagged string (or translation key) into a styled `Component`.
- **`custom-tooltip-api` (Stalemated)** is a *different kind* of library — a fluent per-item tooltip **builder** (`CustomTooltipApi.builder(itemId).dynamicText(...).register()`, `registerPlaceholder`). It has **no** `registerProcessor` / `resolveTagsToComponent*` markdown-resolution API. ❌ Not a fit.
- **Ember's Text API has a Fabric 1.20.1 build** (v2.5.0–2.9.4, TysonTheEmber) with the same XML-style markup parser + effects (rainbow/gradient/etc.). ✅
- **Plan:** add Ember's Text API (Fabric 1.20.1) as a dependency and **reimplement the ~3-method `libtooltips` facade** (`Tooltips.registerProcessor` / `resolveTagsToComponent` / `resolveTagsToComponentFromTranslationKey`) in-repo (package `com.wdiscute.libtooltips`) delegating to Ember's Fabric API. Verify Ember's Fabric API exposes **custom effect/tag registration** for the mod's named gradients; if not, implement those gradients directly (the mod already has the gradient math in `tooltips/SCTooltipGradient`, `SCLegendary`). Confirm bundling is license-permitted (README says Ember jar-in-jar was approved for this mod).

### 7bis.2 `tiny-multiblock-lib` → **official Fabric build (drop-in)**
- Fabric 1.20.1 build exists by **Nikdo53**, same `net.nikdo53.tinymultiblocklib` package. Used by `blocks/aquarium`, `blocks/stand`, `blocks/Telescope`, `TrophyOfTheOlderAngler` (`AbstractMultiBlock`, `IMultiBlock`, `IPreviewableMultiblock`, `AbstractMultiBlockEntity`, `IBlockPosOffsetEnum`). ✅
- **Plan:** swap the build.gradle coordinate `maven.modrinth:tiny-multiblock-lib:forge-1.20.1-…` → the **fabric-1.20.1** artifact; `include(...)` if bundling. Verify the five types' signatures match (high confidence, same author). This **removes the earlier "reimplement multiblock" risk**.

### 7bis.3 `wds-selling-bin` → **DECIDED: downport wd's own Selling-Bin `1.21.1-fabric` → `1.20.1-fabric`** (sub-project)

**Reference found:** [wdiscute/Selling-Bin @ `1.21.1-fabric`](https://github.com/wdiscute/Selling-Bin/tree/1.21.1-fabric) is a **native Fabric rewrite** (~40 classes) in the **exact `com.wdiscute.sellingbin` package** Starcatcher consumes: `SellingBin`, `processors.AbstractProcessor` (+ `SBProcessors`, Food/Enchantment/Durability/QualityFoods/etc.), `registry.SBBlocks`/`SBDataMaps`/`SBMenuTypes`/`SBBlockEntities`/`SBItemPredicate`, `bin.*` (block/BE/menu/screen/slots), `jei.SellingBinJeiPlugin`, `emi.SellingBinEmiPlugin`, `event.SBvents`/`SBClientEvents`, `SBConfig`, `datagen.*`. This is a **bounded, well-scoped sub-project** and doubles as a **reference template** for Starcatcher's own port.

**wd's Fabric tooling choices in that repo (adopt for consistency where sensible):**
- **Fabric Loom** (`fabric-loom` / `-remap` variant — verify exact id), Fabric API.
- **Datamaps Refabricated** (`1.0.5`) for data maps — the Fabric analogue to NeoForge data maps. ⇒ **revises §5.6** (use this lib instead of a hand-rolled loader for Starcatcher's `SCDataMaps` too).
- **Cloth Config** for config (not Forge Config API Port).
- **tiny-multiblock-lib (Fabric)**, **Architectury API** (compile-only), JEI-fabric, EMI-fabric.
- **No** Cardinal Components, **no** Forge Config API Port, **no** NeoBackports (it's a from-scratch Fabric rewrite, not a shim port).

**Downport gap (1.21.1-fabric → 1.20.1-fabric):** the reference targets 1.21.1, where **data components are native**; at 1.20.1 they are not, so the selling-bin's component reads/writes must drop to NBT — the **same DataComponents shim from §5.2 can be shared** between this sub-project and Starcatcher. Also reconcile 1.21↔1.20 API deltas (enchantment/food/item-predicate APIs, `SBItemPredicate`, menu/slot signatures). Confirm the `event.SBvents` built-in-datapack path (Fabric uses `ResourceManagerHelper.registerBuiltinResourcePack`) so Starcatcher's `SCModEvents` pack-finder glue and `SCProcessors` line up with the Fabric selling-bin API (note the Forge branch's `SBevents.DefaultPackSource` may not exist verbatim on Fabric).

**Plan:** (1) fork/branch wd's Selling-Bin to `1.20.1-fabric`, downport using §5.2 shim + version deltas; (2) publish it (Modrinth maven or local `.libs`/`include`); (3) depend on it from Starcatcher and adapt `SCProcessors`/selling-bin glue to the Fabric API. Sequence this **before** Starcatcher's P7/P8 (or in parallel, since it's independent).

---

_(superseded — retained for history)_ Prior "no API-compatible Fabric build" concern:
- Actual API used (specific to **wd's** selling bin): `SellingBin.SELLING_BIN` (a custom `DeferredRegisterTyped` registry) + `SellingBin.rl` ×8, `processors.AbstractProcessor` ×13 (SCProcessors registers `FishProcessor` into it), `SBDataMaps.SELLING_BIN_VALUE`/`SELLING_BIN_CURRENCIES`/`ItemValue` ×7, `SBBlocks.SELLING_BIN`, `SBevents.DefaultPackSource` (built-in datapacks), `jei/StarcatcherJeiPlugin`/`emi` use `SellingBinJeiPlugin.SLOT` ×5, plus two built-in datapacks (`selling_bin_fishes`, `selling_bin_starcatcher_emeralds`).
- The suggested Fabric options are **not** API-compatible: **i5onad3/selling-bin-fabric** is **1.21.1-only**, a different author, GPLv3 independent fork, **different package** (not `com.wdiscute.sellingbin`), simplified block-bound design. The CurseForge "Selling Bin" is likewise not wd's `com.wdiscute.sellingbin` codebase. ❌ Neither is a drop-in.
- **Plan (pick one, confirm with author):**
  - **(a) Port wd's own `wds-selling-bin` to Fabric 1.20.1** (best fidelity; it's the author's mod and itself uses NeoBackports, so it rides the same shim). Preferred if the author will ship a Fabric selling-bin.
  - **(b) Feature-gate the entire selling-bin subsystem** behind `isModLoaded`: drop `SCProcessors`, the `sellingbin/` package, the JEI/EMI selling-bin recipe/slot code, the two built-in datapacks, and the `SBevents.DefaultPackSource` pack finders. Mod ships without selling support on Fabric.
  - **(c) Thin re-facade** of just the used surface against a Fabric selling-bin fork — **not viable** given the fork's different package/design.
- This is the **remaining hard blocker** (§14 R1).

## 8. Compatibility modules (`compat/*`, gated) — ✅ DONE (2026-09-01, P7), except selling bin (→ P8)

Each is `modCompileOnly` + `isModLoaded`-guarded (or, for Trinkets, a hard dependency — see D5 below). All artifact
coordinates below were verified to actually exist for Fabric 1.20.1 (Modrinth/CurseForge API lookups, not guessed)
before being added to `build.gradle`/`gradle.properties`.

- [x] **JEI** (`compat/jei/*`, 4 files): `modCompileOnly "mezz.jei:jei-1.20.1-fabric-api:${jei_version}"` (blamejared maven, already configured). The `@JeiPlugin`/`IModPlugin`/category/registration API is loader-agnostic and needed zero changes beyond the dependency. Two real fixes: (1) `net.nikdo53.neobackports.utils.recipe.holder.RecipeHolder` doesn't exist on Fabric — that NeoForge-backport wrapper around `RecipeManager.getAllRecipesFor(...)` results was never built; vanilla 1.20.1 already returns plain `List<SmithingRecipe>`, so the `instanceof RecipeHolder<?,?> holder` unwrap was simply dropped and the stream filters on `smithingRecipe` directly. (2) `StarcatcherJeiPlugin`/`StarcatcherJeiFPRecipe`/`StarcatcherJeiSmithingRecipe` imported `com.wdiscute.sellingbin.SellingBin`/`SellingBinJeiPlugin` purely to borrow a `ResourceLocation` helper and a decorative slot-background texture from the unported sellingbin companion mod (§7bis.3) — repointed to `Starcatcher.rl(...)`; the texture path (`starcatcher:textures/gui/slot_background.png`) doesn't have a real asset yet and is marked `TODO(P8)` inline, cosmetic-only (missing-texture placeholder, not a crash).
- [x] **EMI** (`compat/emi/*`, 5 files): `modCompileOnly "maven.modrinth:emi:${emi_version}"` (Modrinth maven, already configured — same repo tiny-multiblock-lib/Trinkets/forge-config-api-port use). Same two fixes as JEI: dropped the `RecipeHolder` unwrap (EMI's `getRecipeManager().getAllRecipesFor(...)` also returns plain `SmithingRecipe`s on 1.20.1 vanilla), and repointed `StarcatcherEmiPlugin`'s `SellingBin.rl(...)` info-recipe IDs to `Starcatcher.rl(...)`. `StarcatcherEmiFPRecipe`/`StarcatcherEmiSmithingRecipe`/`IsolatedEmiFPScreen`/`StarcatcherShowInGuideEmiWidget` needed no changes at all — EMI's API was already fully loader-agnostic there.
- [x] **Curios → Trinkets** (`compat/curios/*`, 4 files) (D5): rewritten against the real Trinkets 3.7.2 API (verified via `javap` against the remapped jar in `.gradle/loom-cache`, not guessed). `Trinket` (interface, no base-class conflict with `HatItem extends BlockItem`) replaces `ICurioItem`; `CurioHatItem` now overrides `use(Level, Player, InteractionHand)` and calls the static `TrinketItem.equipItem(player, stack)` helper to replicate Curios' right-click-to-equip (`canEquipFromUse`), since `Trinket` itself has no equivalent hook. `TrinketRenderer`/`TrinketRendererRegistry.registerRenderer(Item, TrinketRenderer)` replace `ICurioRenderer`/`CuriosRendererRegistry` — note the render callback signature changed shape (entity is now a direct parameter, not read off a `SlotContext`). `CuriosCompat.getItems(player)` now reads `TrinketsApi.getTrinketComponent(player)` → `TrinketComponent.getAllEquipped()` instead of Curios' `IItemHandlerModifiable`. **Slot data**: deleted `data/curios/tags/item/head.json` and `data/starcatcher/curios/**` (Curios' custom-slot-definition format); added `data/trinkets/tags/items/head/hat.json` tagging the 16 fisherman-hat items into Trinkets' own **built-in** `head`/`hat` slot (Trinkets ships default slot groups — `head/hat`, `head/face`, `chest/necklace`, etc. — so no custom slot definition was needed, just a tag). Trinkets was already a hard `modImplementation` dependency from P0 (D5), not an optional `isModLoaded`-gated one like Curios was — so the three `isModLoaded("curios")` call-site guards (`SCClientEvents.registerRenderers`, `SCCatchModifiers.getCatchModifiers`, `SCMinigameModifiers.getMinigameModifiers`) were removed as dead branching, not translated to `isModLoaded("trinkets")`. **Pre-existing, not fixed**: `CurioHatItem` is never actually instantiated anywhere — `SCBlocks.registerHat(...)` registers hats as plain `HatItem`, not `CurioHatItem` — this looks like already-incomplete/vestigial wiring from before the port started, out of scope to fix here (1:1 preservation, not a bug hunt).
- [x] **Serene Seasons**: real Fabric 1.20.1 build confirmed (Modrinth `serene-seasons`, version `9.1.0.3`) → `modCompileOnly "maven.modrinth:serene-seasons:${serene_seasons_version}"`. `SereneSeasonsCompat.java` needed zero code changes — it already only referenced the loader-agnostic `sereneseasons.api.season.*` package.
- [x] **Ecliptic Seasons**: confirmed (Modrinth/CurseForge search, 2026-09-01) there is **no Fabric 1.20.1 build** — "Ecliptic Seasons: Fabricated" is a from-scratch Fabric rewrite targeting MC 26.1+, not 1.20.1. Since `"eclipticseasons"` can therefore never be `isModLoaded` on this Fabric build, **gated off**: deleted `EclipticSeasonsCompat.java` (its body only compiled against that nonexistent API) and its dead branch in `SeasonRestriction.getFishChance`; the harmless `isModLoaded("eclipticseasons")` string check in `isEnabled()` (no class dependency) was left in place — forward-compatible if a real Fabric build ever ships.
- [x] **TerraFirmaCraft**: confirmed no Fabric port of the real mod exists (only the from-scratch "TerraFabriCraft" spiritual successor, unrelated API) → **gated off** the same way as Ecliptic Seasons: deleted `TerraFirmaCraftSeasonsCompat.java` and its dead branch in `SeasonRestriction`.
- [x] **FTB Teams**: no Modrinth listing, but a real CurseForge Fabric 1.20.1 build exists (`ftb-teams-fabric`, project 438497, file 7499808 = version 2001.3.2) → `modCompileOnly "curse.maven:ftb-teams-fabric-438497:${ftb_teams_fabric_file}"` (cursemaven repo already configured in P0). `FTBTeamsCompat.java` needed zero code changes.
- [x] **Iris/Oculus** (`IrisShadersCompat`): confirmed Fabric 1.20.1 build (Modrinth `iris`, version `1.7.6+1.20.1`) → `modCompileOnly "maven.modrinth:iris:${iris_version}"`. The file already used `net.fabricmc.loader.api.FabricLoader.isModLoaded("iris")` and the real `net.irisshaders.iris.*` packages (Iris is Fabric-native; Oculus is the separate Forge port) — it was already fully Fabric-shaped and only needed the dependency added, zero code changes.
- [x] **Quality Food / Reliquified Artifacts** (`QualityFoodCompat`, `ReliquifiedArtifactsCompat`): confirmed both mods are Forge/NeoForge-only with no Fabric build at all → **gated off**. `QualityFoodCompat.java` deleted, its two call sites in `FishProperties.makeItemStack` (both `isModLoaded("quality_food")`-guarded) reduced to a comment. `ReliquifiedArtifactsCompat.java` kept (its 3 public methods are called unconditionally-per-branch from `AnglersHatModifier`, all `isModLoaded("reliquified_artifacts")`-guarded there) but its private Relics-API helpers (`getEquippedHatStack`/`getCatchAbility`, plus their imports) were deleted since they compiled against an API that doesn't exist on Fabric and were only ever called from already-commented-out code — the public methods were already stubs (`shouldAwardBonusTreasure` → `true`, others → no-op/`List.of()`) and needed no change.
- [ ] **Selling bin** (`sellingbin`/`SCProcessors`, built-in datapacks `selling_bin_*`, `data_maps`): **not attempted, tracked under P8** — see **§7bis.3**. This is the one item in this section that isn't a simple "swap artifact or gate" call; it needs either a real Fabric port of wd's own selling-bin mod or a full feature-gate of the subsystem, and several other deferred items (`DGSCDataMapsProvider` from P6, `SCModEvents.addPackFinders` from P4) are blocked on the same decision.

---

## 9. Data generation (13 providers) — ✅ DONE (2026-09-01, P6), except `DGSCDataMapsProvider`

Forge datagen (`GatherDataEvent`, `ExistingFileHelper`, `DatapackBuiltinEntriesProvider`, `BlockTagsProvider`, `IGlobalLootModifier` provider) → **Fabric Data Generation API**. All real class names below confirmed via `javap` against the actual `fabric-data-generation-api-v1` module jar and the vanilla Loom-mapped jar, not guessed.

**Key realization that reframed this whole phase**: `src/generated/resources` (1049 files) is already fully populated from the last Forge datagen run and committed as static resources — the running mod works off those regardless of whether the datagen source code itself compiles or has been re-run. So this phase's bar was "make the datagen entrypoint compile and be usable again," not "verify every provider's output byte-for-byte" (that's closer to P9 QA territory) — several conversions below lean on that to justify not reproducing every original code path 1:1.

- [x] `DataGenerators` (Forge `@SubscribeEvent GatherDataEvent`) → new `SCDataGenerator implements DataGeneratorEntrypoint`, registered under a `"fabric-datagen"` key in `fabric.mod.json`. `RegistrySetBuilder.add(FISH_REGISTRY_KEY, ...)` (previously inline in the gather-data method) moved to the entrypoint's own `buildRegistry(RegistrySetBuilder)` hook, which Fabric guarantees runs *before* `onInitializeDataGenerator` — so `FishingPropertiesRegistry.register()` (which populates the `PROPERTIES` list every other provider reads) is called there and is safely populated by the time any provider's `addTags`/`configure` runs.
- [x] Vanilla `TagsProvider`/`ItemTagsProvider`/`BiomeTagsProvider` have **no** modid/`ExistingFileHelper` constructor params and **no** `addOptional`/`addOptionalTag` sugar at all under Fabric — those are pure Forge patches. Fabric's own `FabricTagProvider<T>` (base class) + its `ItemTagProvider`/`BlockTagProvider` inner specializations restore equivalent sugar via `getOrCreateTagBuilder(TagKey<T>)` → `FabricTagBuilder` (`add`/`addOptional`/`addTag`/`addOptionalTag`/`remove`, same shapes as Forge's). There is **no** `BiomeTagProvider`/`FishPropertiesTagProvider` inner specialization, so `DGSCBiomeTagsProvider`/`DGSCFPTagsProvider` extend the base `FabricTagProvider<Biome>`/`FabricTagProvider<FishProperties>` directly instead.
- [x] `net.minecraft.data.tags.BlockTagsProvider` **does not exist in vanilla at all** (confirmed via `javap`, "class not found") — Forge invented it as an `IntrinsicHolderTagsProvider<Block>` convenience wrapper mirroring `ItemTagsProvider`. `DGSCBlocksTagsProvider` now extends Fabric's `FabricTagProvider.BlockTagProvider` instead (its `addTags` body was already empty, so this was a pure base-class swap).
- [x] `Tags.Biomes.IS_MUSHROOM/IS_SWAMP/IS_DESERT` and `Tags.Items.DYES/GLASS_PANES` (Forge's `c:` conventional tags) → Fabric's `net.fabricmc.fabric.api.tag.convention.v1.ConventionalBiomeTags.MUSHROOM/SWAMP/DESERT` and `ConventionalItemTags.DYES/GLASS_PANES` — same `c:` cross-loader convention, different constant names (no `IS_` prefix on the Fabric side). `Tags.Items.TOOLS_FISHING_ROD` had no direct Fabric equivalent, but that call site was already commented out.
- [x] `DGSCAdvancementProvider extends ForgeAdvancementProvider` → vanilla's own `AdvancementProvider(PackOutput, CompletableFuture<HolderLookup.Provider>, List<AdvancementSubProvider>)` — trivial, since the original was already calling it with an empty `List.of()` (this mod's advancement JSONs are hand-authored static resources, not code-generated).
- [x] `DGSCBiomeModifierProvider` — **deleted entirely**. Its whole body was already commented out under Forge (zero `context.register(...)` calls), so it generated nothing; no Fabric Biome Modification API work was needed to replace a no-op.
- [x] `DGSCBlockLootTableProvider extends BlockLootSubProvider` (wrapped in a `LootTableProvider.SubProviderEntry` under Forge) → Fabric's `FabricBlockLootTableProvider`, which extends the same vanilla `BlockLootSubProvider` and *is itself* a full `DataProvider` — no wrapping needed, `generate()` just widens from `protected` to `public` to match Fabric's abstract signature.
- [x] `DGSCFishingPropertiesProvider extends DatapackBuiltinEntriesProvider` (Forge) → `FabricDynamicRegistryProvider`, whose `configure(HolderLookup.Provider, Entries)` calls `entries.addAll(registries.lookupOrThrow(FISH_REGISTRY_KEY))`. **Known gap, documented not silently dropped**: the original's `registerConditions`/`ModLoadedCondition` gated every cross-mod fish entry (aquaculture, tide, etc.) behind "only emit this JSON if that mod is loaded" — `FabricDynamicRegistryProvider` has no per-entry condition hook at all, so that datagen-time optimization is lost (not a runtime crash risk, the registry codec round-trips fine regardless; just means dev-generated datapacks may reference fish tied to mods a given server doesn't have). Removed the now-dead `FishingPropertiesRegistry.registerConditions`/`ICondition`/`ModLoadedCondition` imports that this left behind.
- [x] `DGSCItemModelProvider` (Forge's `ItemModelProvider`/`ItemModelBuilder`, `withExistingParent(...).texture(...)`) → Fabric's `FabricModelProvider` + vanilla's own `ItemModelGenerators.generateFlatItem(Item, ModelTemplate)` (`ModelTemplates.FLAT_ITEM`), which produces the identical `item/generated` + `layer0` JSON shape `simpleItem` used to hand-build. The **block-item half** (`simpleBlockItem` calls, parenting an item model to the block's own model) was **not** reproduced — this project has no block-state datagen provider at all, so those item models were always hand-authored/one-time-generated static files, not part of a regenerable pipeline; they already exist under `src/generated/resources` per this section's opening note.
- [x] `DGSCRecipeProvider`, `DGSCItemsTagsProvider` — needed only the `Tags.Items.*` → `ConventionalItemTags.*` swap noted above; both already extended plain vanilla `RecipeProvider`/used vanilla tag machinery under Forge, no other changes.
- [ ] `DGSCDataMapsProvider` → **not converted, left as-is (still broken)**. Needs two things this phase didn't build: (1) a `net.nikdo53.neobackports.datagen.DataMapProvider` datagen-side shim emitting the custom data-map JSON format from §5.6 (only the runtime-loading half, `DataMapType`/`DataMapRegistry`, exists so far) — real work, not a mechanical port; (2) most of its actual content (`SBDataMaps.SELLING_BIN_CURRENCIES/VALUE`, `FishProcessor`, `QualityFoodsProcessor`) lives in the still-unported `com.wdiscute.sellingbin` companion mod (§7bis.3/P8). Tracked as a follow-up once the selling-bin downport lands.
- [x] `DGSCLootModifiers`/`AddItemModifier` — already removed in P2 (§7), logic moved to `LootTableEvents.MODIFY`; nothing left to do here.

Note: much of `src/generated/resources` (fish JSON, tags, models) is **loader-neutral data** and can be reused as-is; only Forge-namespaced files (`data/forge/**`, `data/neoforge/**`) needed conversion — already handled in earlier phases (loot modifiers → runtime code in P2, data maps → custom `data_maps` format in P4).

---

## 10. Mixins (4 mixins)

- [ ] `starcatcher.mixins.json` works on Fabric; ensure `package`, `compatibilityLevel: JAVA_17`, add `refmap`. Split `RenderTypeHelperMixin` (client) into a `client` array (already present).
- [ ] Verify each mixin target resolves under Mojmap+Parchment: `GetNameMixin`, `LecternReturnMixin`, `RemoveFishSizeAndWeightWhenStacking` (must match the NBT-backed component semantics from §5.2), `RenderTypeHelperMixin`.
- [ ] `overlayMessageTime` write (used by the toast helper) now via access widener instead of AT.

---

## 11. Resources & assets

- [ ] Assets (textures, models, blockstates, sounds, particles, shaders, sprites, lang×9) are **loader-neutral** — copy unchanged.
- [ ] `data/forge/**` and `data/neoforge/**` → convert to `c:`/vanilla tags or code (§9). Item/block tag files using `forge:` namespaces → `c:` conventional equivalents.
- [x] `data/curios/**` → Trinkets slot data (§8, D5) — done (P7): replaced with `data/trinkets/tags/items/head/hat.json`.
- [ ] Built-in datapacks (`built_in_datapacks/selling_bin_*`) → registered via Fabric builtin resource pack API (§6) or dropped with selling-bin gating (§8).
- [ ] `pack.mcmeta` present/valid for 1.20.1 pack format.

---

## 12. Build, run, verify

- [ ] `./gradlew build` compiles; `runClient`/`runServer`/`runDatagen` configs via Loom.
- [ ] Smoke test matrix: world load; fishing minigame start→complete→catch payload roundtrip; toast + overlay message; guide book screen; tackle box menu (extended screen data); aquarium multiblock place/preview (D7); tournament stand (saved data + sync payloads); creative tabs populated; config screen values; message-in-a-bottle set/read payloads.
- [ ] Attachment persistence: guide received flag copy-on-death/respawn; tackle skin on bobber synced to client; fishing-bob state.
- [ ] Data-driven: a dynamic FishProperties datapack entry loads and syncs to client.
- [ ] Each compat module: load with the corresponding Fabric mod present and absent (gating correctness).
- [ ] Loot: fishing/treasure drops from the reimplemented `LootTableEvents` modifiers.

---

## 13. Suggested phase ordering (each phase should compile before the next)

1. **P0 Scaffolding** — ✅ done (2026-08-31, branch `fabric-port-1.20.1`). §2 build, §3 metadata/AW, §4 empty entrypoints. Project imports on Fabric; `compileJava` fails only on the untouched Forge-API source files, as expected.
2. **P1 Shim core** — ✅ done (2026-08-31). §5.1 registries/Deferred*, §5.3 codecs, §5.8 misc utils all done and verified against the real compiler (full-project error count 3554→682→619 lines of output across the three sub-phases). `SCItems`/`SCBlocks`/`SCBlockEntities`/`SCEntities`/`SCSounds`/`SCParticles`/`SCMenuTypes`/`SCCreativeModeTabs`/`SCCriterionTriggers`/`SCRecipes` wired into `StarcatcherFabric.onInitialize()`. Custom registries and `ForgeRegistries`/`NeoForgeRegistries` lookups (§5.1's two remaining unchecked boxes) stay deferred to P4 as planned.
3. **P2 Components & stacks** — ✅ done (2026-09-01). §5.2 (D1) done 2026-08-31 (error count 619→555 including a bundled jsr305 fix); §7 recipes/menus done in §5.8; §7 creative tabs (needed no changes), loot modifiers (`LootTableEvents.MODIFY`), and the self-contained inventory sites (`SimpleContainer`/`Slot`) done 2026-09-01 (error count 555→535). Two inventory sites deliberately deferred — `TackleBoxBlockEntity`'s Forge Capability (needs Fabric Transfer API, not a 1:1 swap) and `CuriosCompat` (needs the Curios→Trinkets swap, §8/D5) — neither blocks anything else. Mixins (§10) not yet reviewed.
4. **P3 Networking & attachments** — ✅ done (2026-09-01). §5.4 (D4) payload registrar/distributor over Fabric Networking API v1; §5.5 (D2) attachments over Cardinal Components. Error count 535→423 (−112). Mixins (§10) not yet reviewed.
5. **P4 Events & world systems** — ✅ done (2026-09-01), except the two explicitly-deferred items noted below. Custom registries (§5.1, `SCCustomRegistries`), dynamic registry (§5.7, `SCDynamicRegistries`), server-side/common events (§6, `SCModEvents`/`SCEvents`), and data maps (§5.6, reimplemented from scratch rather than vendoring the unpublished Datamaps Refabricated — see §5.6 for why) all done; also flattened `ModList.get().isLoaded(id)` → `FabricLoader.getInstance().isModLoaded(id)` across 16 files (§6's own table item). Error count 423→368→358 across the three sub-steps (ModList+registries+events, then data maps). Still open, both deliberately: `AddPackFindersEvent` (blocked on the not-yet-downported selling-bin companion mod, §7bis.3/P8) and `ItemAttributeModifierEvent` (needs a design decision per §5.6, not a mechanical port).
6. **P5 Client** — ✅ done (2026-09-01). Renderers, block-entity renderers, screens, model layers, keymaps (+ tick-polled key handling), particle factories, item properties, core shaders, HUD overlays, tooltip components, item tooltips, and the `@OnlyIn`/`Dist`/`DistExecutor`→`@Environment` sweep (§4/§6). Error count 358→274 (−84); everything left is cleanly attributable to P6 (datagen), P7 (compat), P8 (companion mods), or the still-open `Starcatcher.java` strip (§4 P1+).
7. **P6 Datagen** — ✅ done (2026-09-01), except `DGSCDataMapsProvider` (§9 — blocked on a new `DataMapProvider` datagen shim plus the unported selling-bin companion mod). New `SCDataGenerator` entrypoint (`"fabric-datagen"` in `fabric.mod.json`) wired to `FabricTagProvider`/`FabricModelProvider`/`FabricBlockLootTableProvider`/`FabricDynamicRegistryProvider`; `DGSCBiomeModifierProvider` deleted (dead body under Forge too). Error count 274→227 (−47).
8. **P7 Compat** — ✅ done (2026-09-01), except the selling-bin subsystem (tracked under P8). All 9 non-selling-bin §8 entries resolved: JEI, EMI, Serene Seasons, FTB Teams, and Iris got real verified Fabric-1.20.1 dependency coordinates added (`modCompileOnly`); Ecliptic Seasons, TerraFirmaCraft, Quality Food, and Reliquified Artifacts were confirmed to have no Fabric build at all and gated off (dead compat classes deleted, call sites trimmed); Curios→Trinkets was rewritten against the real Trinkets 3.7.2 API (verified via `javap`). Error count 227→109 (deps added) →78 (compat code fixes); the ~78 remaining are pre-existing and out of scope for §8 (Starcatcher.java strip, TackleBoxBlockEntity's Transfer API, sellingbin/P8, DGSCDataMapsProvider, libtooltips/§7bis.1, and a handful of small pre-existing NeoBackports-shim/mixin gaps §10 hasn't reviewed yet).
9. **P8 Companion-mod resolution** — §7bis: swap tiny-multiblock-lib (Fabric); reimplement libtooltips facade on Ember's Text API (Fabric); **downport wd's Selling-Bin `1.21.1-fabric`→`1.20.1-fabric` as a parallel sub-project** (own repo/branch, shares the §5.2 component shim), then depend on it. Start the selling-bin downport early — it is independent of Starcatcher P1–P6.
10. **P9 QA** — §12 full test matrix; fix mapping/behavior gaps.

---

## 14. Top risks / likely blockers

- **R1 (retired → now a scoped sub-task, see §7bis):** tiny-multiblock-lib ✅ Fabric drop-in; libtooltips ✅ facade on Ember's Text API (Fabric 1.20.1); **wds-selling-bin ✅ decided** — downport wd's own `1.21.1-fabric` branch to `1.20.1-fabric` (same `com.wdiscute.sellingbin` package; native Fabric rewrite that also serves as the port template). Residual risk is the **1.21.1→1.20.1 downport gap** (native data components → NBT shim, API deltas), tracked in §7bis.3 and shared with §5.2.
- **R2 (high):** Data Components (D1) are emulated on NBT by NeoBackports; the Fabric shim must exactly reproduce stacking/equality/serialization semantics or items will mis-stack and the `RemoveFishSizeAndWeightWhenStacking` mixin will misbehave.
- **R3 (med):** NeoForge **data maps** and the `ItemAttributeModifierEvent` lazy-copy pattern have no Fabric analogue → custom loader + relocation of the copy logic (§5.6).
- **R4 (med):** Global Loot Modifiers → hand-reimplement as `LootTableEvents.MODIFY`; parity of conditions/rolls needs care (§7).
- **R5 (retired, P7):** Curios→Trinkets slot/renderer semantics differ (D5) — ported (§8); real semantic delta worth remembering: Trinkets' render callback receives the wearer as a direct parameter instead of via a `SlotContext`, and the fisherman hats now sit in Trinkets' pre-existing built-in `head/hat` slot (tagged in) rather than a mod-defined custom slot like Curios used.
- **R6 (low/med):** Dynamic registry client-sync for `FishProperties` under Fabric; verify large registry syncs.
- **R7 (low):** Mapping mismatches in mixins/AW under Parchment; resolve per-target.

---

## 15. Out of scope / to confirm with author

- Whether to **bundle** (Loom `include`) the Fabric companion libs or hard-depend.
- Whether TFC/QualityFood/Relics/Ecliptic compat is **dropped** on Fabric if no Fabric build exists.
- Version string scheme (`…-FABRIC-1.20.1`) and separate branch (`v2.3-fabric-1.20.1`).
